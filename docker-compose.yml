version: '3.8'

services:
  tomato-fresh:
    image: amaha24/tomato-fresh:latest
    container_name: tomato-fresh-production
    ports:
      - "5001:5001"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped