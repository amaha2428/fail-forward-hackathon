version: '3.8'

services:
  tomato-fresh:
    image: ${DOCKERHUB_USERNAME}/tomato-fresh:${IMAGE_TAG}
    container_name: tomato-fresh-production
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped